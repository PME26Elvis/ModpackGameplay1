# 1. 忽略根目錄下的所有檔案與資料夾
/*

# 2. 【例外】我要保留 saves 資料夾 (包含裡面所有東西)
!/saves/

# 3. 【例外】保留 Git 設定檔與備份腳本
!/.gitignore
!/.gitattributes
!/backup.bat

# 4. 【玩家設定備份】(這次新增的部分)
# 保留 Minecraft 基礎設定 (按鍵、畫面、聲音)
!/options.txt

# 保留模組清單列表
!/modlist.html

# 保留 config 資料夾 (包含 JEI 書籤、所有模組的設定檔)
!/config/

# 保留 local 資料夾 (包含 FTB Chunks 的客戶端地圖快取/小地圖資料)
!/local/